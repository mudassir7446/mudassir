/**
 * * This program shows the output when the variable is only declared and prited
 *
 * Variable x is initialized to 0
 */
#include <stdio.h>
int main(){
int x;
printf("Value of x : %d",x);
getchar();
return 0;
}
